[project]
name = "lab6-contexte-et-etat"
version = "0.1.0"
description = "LAB: L'état et le contexte d'un agent"
requires-python = ">=3.10"
dependencies = [
    "langchain",
    "langchain-ollama",
    "langgraph",
    "python-dotenv",
]

[tool.uv]
package = false
